
  # University Competition Platform UI

  This is a code bundle for University Competition Platform UI. The original project is available at https://www.figma.com/design/Rnt6UKxwfC8zpPnd24BxTX/University-Competition-Platform-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  